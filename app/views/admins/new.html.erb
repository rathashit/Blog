<% @page_title = "New Admin"%>
<div class="page-form">
      <div class="buttons">
            <div class="back-link">
                  <%= link_to("Back to Admins", {:action => 'index'}) %>
            </div>
      </div>
	<div class="form">
		<%= form_for(:admin,:html => {:multipart => true}, :url => {:action => 'create'}) do |f| %>
                  <div id="inner-form">
                        <%= render(:partial => 'form', :locals => {:f => f}) %>       
                  </div>
                  <div class="buttons">
                        <%= submit_tag("Create Admin") %>
                  </div>
            <% end %>
	</div>
</div>